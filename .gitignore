# WordPress / build artifacts
*.log
.DS_Store
node_modules/
# Composer dev dependencies only (root). Anchored so bundled libs such as
# lib/plugin-update-checker/vendor/ (Parsedown) are NOT excluded.
/vendor/
/wp-content/
.phpunit.result.cache
